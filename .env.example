# Public browser setting. Contains no credentials.
VITE_COLYSEUS_URL=https://de-fra-24270fd2.colyseus.cloud
# Optional backend preview origins (comma-separated, exact HTTPS origins).
# The production game origin is always permitted. Localhost is development-only.
ALLOWED_PREVIEW_ORIGINS=
